[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # React app will use environment variables from Netlify UI
  NODE_VERSION = "18"
